export function getFirebaseConfig() {
    const firebaseConfig = null; // add your Firebase config object here

    if (!firebaseConfig) {
        throw new Error('Add your Firebase Config object to firebaseConfig.ts to try this demo');
    }

    return firebaseConfig;
}